import { FrappeGanttDirective } from './frappe-gantt.directive';

describe('FrappeGanttDirective', () => {
  it('should create an instance', () => {
    const directive = new FrappeGanttDirective();
    expect(directive).toBeTruthy();
  });
});
